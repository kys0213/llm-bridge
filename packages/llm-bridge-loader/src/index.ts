// Ensure re-export remains stable across build targets
export { DependencyBridgeLoader } from './dependency/dependency-bridge.loader';
export type { BridgeLoader, BridgeLoadResult, ScanOptions } from './types';
